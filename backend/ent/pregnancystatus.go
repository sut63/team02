// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"github.com/facebook/ent/dialect/sql"
	"github.com/to63/app/ent/pregnancystatus"
)

// Pregnancystatus is the model entity for the Pregnancystatus schema.
type Pregnancystatus struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Pregnancystatus holds the value of the "Pregnancystatus" field.
	Pregnancystatus string `json:"Pregnancystatus,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the PregnancystatusQuery when eager-loading is set.
	Edges PregnancystatusEdges `json:"edges"`
}

// PregnancystatusEdges holds the relations/edges for other nodes in the graph.
type PregnancystatusEdges struct {
	// Antenatalinformation holds the value of the Antenatalinformation edge.
	Antenatalinformation []*Antenatalinformation
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// AntenatalinformationOrErr returns the Antenatalinformation value or an error if the edge
// was not loaded in eager-loading.
func (e PregnancystatusEdges) AntenatalinformationOrErr() ([]*Antenatalinformation, error) {
	if e.loadedTypes[0] {
		return e.Antenatalinformation, nil
	}
	return nil, &NotLoadedError{edge: "Antenatalinformation"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Pregnancystatus) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case pregnancystatus.FieldID:
			values[i] = &sql.NullInt64{}
		case pregnancystatus.FieldPregnancystatus:
			values[i] = &sql.NullString{}
		default:
			return nil, fmt.Errorf("unexpected column %q for type Pregnancystatus", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Pregnancystatus fields.
func (pr *Pregnancystatus) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case pregnancystatus.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			pr.ID = int(value.Int64)
		case pregnancystatus.FieldPregnancystatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field Pregnancystatus", values[i])
			} else if value.Valid {
				pr.Pregnancystatus = value.String
			}
		}
	}
	return nil
}

// QueryAntenatalinformation queries the "Antenatalinformation" edge of the Pregnancystatus entity.
func (pr *Pregnancystatus) QueryAntenatalinformation() *AntenatalinformationQuery {
	return (&PregnancystatusClient{config: pr.config}).QueryAntenatalinformation(pr)
}

// Update returns a builder for updating this Pregnancystatus.
// Note that you need to call Pregnancystatus.Unwrap() before calling this method if this Pregnancystatus
// was returned from a transaction, and the transaction was committed or rolled back.
func (pr *Pregnancystatus) Update() *PregnancystatusUpdateOne {
	return (&PregnancystatusClient{config: pr.config}).UpdateOne(pr)
}

// Unwrap unwraps the Pregnancystatus entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (pr *Pregnancystatus) Unwrap() *Pregnancystatus {
	tx, ok := pr.config.driver.(*txDriver)
	if !ok {
		panic("ent: Pregnancystatus is not a transactional entity")
	}
	pr.config.driver = tx.drv
	return pr
}

// String implements the fmt.Stringer.
func (pr *Pregnancystatus) String() string {
	var builder strings.Builder
	builder.WriteString("Pregnancystatus(")
	builder.WriteString(fmt.Sprintf("id=%v", pr.ID))
	builder.WriteString(", Pregnancystatus=")
	builder.WriteString(pr.Pregnancystatus)
	builder.WriteByte(')')
	return builder.String()
}

// Pregnancystatuses is a parsable slice of Pregnancystatus.
type Pregnancystatuses []*Pregnancystatus

func (pr Pregnancystatuses) config(cfg config) {
	for _i := range pr {
		pr[_i].config = cfg
	}
}
