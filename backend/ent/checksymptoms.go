// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"github.com/facebook/ent/dialect/sql"
	"github.com/to63/app/ent/checksymptoms"
	"github.com/to63/app/ent/disease"
	"github.com/to63/app/ent/doctorordersheet"
	"github.com/to63/app/ent/patient"
	"github.com/to63/app/ent/personnel"
)

// Checksymptoms is the model entity for the Checksymptoms schema.
type Checksymptoms struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Date holds the value of the "date" field.
	Date time.Time `json:"date,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ChecksymptomsQuery when eager-loading is set.
	Edges                ChecksymptomsEdges `json:"edges"`
	_Disease_id          *int
	_DoctorOrderSheet_id *int
	_Patient_id          *int
	_Personnel_id        *int
}

// ChecksymptomsEdges holds the relations/edges for other nodes in the graph.
type ChecksymptomsEdges struct {
	// Disease holds the value of the disease edge.
	Disease *Disease
	// Patient holds the value of the patient edge.
	Patient *Patient
	// Personnel holds the value of the personnel edge.
	Personnel *Personnel
	// Doctorordersheet holds the value of the doctorordersheet edge.
	Doctorordersheet *DoctorOrderSheet
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [4]bool
}

// DiseaseOrErr returns the Disease value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ChecksymptomsEdges) DiseaseOrErr() (*Disease, error) {
	if e.loadedTypes[0] {
		if e.Disease == nil {
			// The edge disease was loaded in eager-loading,
			// but was not found.
			return nil, &NotFoundError{label: disease.Label}
		}
		return e.Disease, nil
	}
	return nil, &NotLoadedError{edge: "disease"}
}

// PatientOrErr returns the Patient value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ChecksymptomsEdges) PatientOrErr() (*Patient, error) {
	if e.loadedTypes[1] {
		if e.Patient == nil {
			// The edge patient was loaded in eager-loading,
			// but was not found.
			return nil, &NotFoundError{label: patient.Label}
		}
		return e.Patient, nil
	}
	return nil, &NotLoadedError{edge: "patient"}
}

// PersonnelOrErr returns the Personnel value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ChecksymptomsEdges) PersonnelOrErr() (*Personnel, error) {
	if e.loadedTypes[2] {
		if e.Personnel == nil {
			// The edge personnel was loaded in eager-loading,
			// but was not found.
			return nil, &NotFoundError{label: personnel.Label}
		}
		return e.Personnel, nil
	}
	return nil, &NotLoadedError{edge: "personnel"}
}

// DoctorordersheetOrErr returns the Doctorordersheet value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ChecksymptomsEdges) DoctorordersheetOrErr() (*DoctorOrderSheet, error) {
	if e.loadedTypes[3] {
		if e.Doctorordersheet == nil {
			// The edge doctorordersheet was loaded in eager-loading,
			// but was not found.
			return nil, &NotFoundError{label: doctorordersheet.Label}
		}
		return e.Doctorordersheet, nil
	}
	return nil, &NotLoadedError{edge: "doctorordersheet"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Checksymptoms) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case checksymptoms.FieldID:
			values[i] = &sql.NullInt64{}
		case checksymptoms.FieldDate:
			values[i] = &sql.NullTime{}
		case checksymptoms.ForeignKeys[0]: // _Disease_id
			values[i] = &sql.NullInt64{}
		case checksymptoms.ForeignKeys[1]: // _DoctorOrderSheet_id
			values[i] = &sql.NullInt64{}
		case checksymptoms.ForeignKeys[2]: // _Patient_id
			values[i] = &sql.NullInt64{}
		case checksymptoms.ForeignKeys[3]: // _Personnel_id
			values[i] = &sql.NullInt64{}
		default:
			return nil, fmt.Errorf("unexpected column %q for type Checksymptoms", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Checksymptoms fields.
func (c *Checksymptoms) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case checksymptoms.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			c.ID = int(value.Int64)
		case checksymptoms.FieldDate:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field date", values[i])
			} else if value.Valid {
				c.Date = value.Time
			}
		case checksymptoms.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field _Disease_id", value)
			} else if value.Valid {
				c._Disease_id = new(int)
				*c._Disease_id = int(value.Int64)
			}
		case checksymptoms.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field _DoctorOrderSheet_id", value)
			} else if value.Valid {
				c._DoctorOrderSheet_id = new(int)
				*c._DoctorOrderSheet_id = int(value.Int64)
			}
		case checksymptoms.ForeignKeys[2]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field _Patient_id", value)
			} else if value.Valid {
				c._Patient_id = new(int)
				*c._Patient_id = int(value.Int64)
			}
		case checksymptoms.ForeignKeys[3]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field _Personnel_id", value)
			} else if value.Valid {
				c._Personnel_id = new(int)
				*c._Personnel_id = int(value.Int64)
			}
		}
	}
	return nil
}

// QueryDisease queries the "disease" edge of the Checksymptoms entity.
func (c *Checksymptoms) QueryDisease() *DiseaseQuery {
	return (&ChecksymptomsClient{config: c.config}).QueryDisease(c)
}

// QueryPatient queries the "patient" edge of the Checksymptoms entity.
func (c *Checksymptoms) QueryPatient() *PatientQuery {
	return (&ChecksymptomsClient{config: c.config}).QueryPatient(c)
}

// QueryPersonnel queries the "personnel" edge of the Checksymptoms entity.
func (c *Checksymptoms) QueryPersonnel() *PersonnelQuery {
	return (&ChecksymptomsClient{config: c.config}).QueryPersonnel(c)
}

// QueryDoctorordersheet queries the "doctorordersheet" edge of the Checksymptoms entity.
func (c *Checksymptoms) QueryDoctorordersheet() *DoctorOrderSheetQuery {
	return (&ChecksymptomsClient{config: c.config}).QueryDoctorordersheet(c)
}

// Update returns a builder for updating this Checksymptoms.
// Note that you need to call Checksymptoms.Unwrap() before calling this method if this Checksymptoms
// was returned from a transaction, and the transaction was committed or rolled back.
func (c *Checksymptoms) Update() *ChecksymptomsUpdateOne {
	return (&ChecksymptomsClient{config: c.config}).UpdateOne(c)
}

// Unwrap unwraps the Checksymptoms entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (c *Checksymptoms) Unwrap() *Checksymptoms {
	tx, ok := c.config.driver.(*txDriver)
	if !ok {
		panic("ent: Checksymptoms is not a transactional entity")
	}
	c.config.driver = tx.drv
	return c
}

// String implements the fmt.Stringer.
func (c *Checksymptoms) String() string {
	var builder strings.Builder
	builder.WriteString("Checksymptoms(")
	builder.WriteString(fmt.Sprintf("id=%v", c.ID))
	builder.WriteString(", date=")
	builder.WriteString(c.Date.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// ChecksymptomsSlice is a parsable slice of Checksymptoms.
type ChecksymptomsSlice []*Checksymptoms

func (c ChecksymptomsSlice) config(cfg config) {
	for _i := range c {
		c[_i].config = cfg
	}
}
