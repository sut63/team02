// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"github.com/facebook/ent/dialect/sql"
	"github.com/to63/app/ent/dentaltype"
)

// Dentaltype is the model entity for the Dentaltype schema.
type Dentaltype struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Typename holds the value of the "typename" field.
	Typename string `json:"typename,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the DentaltypeQuery when eager-loading is set.
	Edges DentaltypeEdges `json:"edges"`
}

// DentaltypeEdges holds the relations/edges for other nodes in the graph.
type DentaltypeEdges struct {
	// Dentalappointment holds the value of the Dentalappointment edge.
	Dentalappointment []*Dentalappointment
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// DentalappointmentOrErr returns the Dentalappointment value or an error if the edge
// was not loaded in eager-loading.
func (e DentaltypeEdges) DentalappointmentOrErr() ([]*Dentalappointment, error) {
	if e.loadedTypes[0] {
		return e.Dentalappointment, nil
	}
	return nil, &NotLoadedError{edge: "Dentalappointment"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Dentaltype) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case dentaltype.FieldID:
			values[i] = &sql.NullInt64{}
		case dentaltype.FieldTypename:
			values[i] = &sql.NullString{}
		default:
			return nil, fmt.Errorf("unexpected column %q for type Dentaltype", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Dentaltype fields.
func (d *Dentaltype) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case dentaltype.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			d.ID = int(value.Int64)
		case dentaltype.FieldTypename:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field typename", values[i])
			} else if value.Valid {
				d.Typename = value.String
			}
		}
	}
	return nil
}

// QueryDentalappointment queries the "Dentalappointment" edge of the Dentaltype entity.
func (d *Dentaltype) QueryDentalappointment() *DentalappointmentQuery {
	return (&DentaltypeClient{config: d.config}).QueryDentalappointment(d)
}

// Update returns a builder for updating this Dentaltype.
// Note that you need to call Dentaltype.Unwrap() before calling this method if this Dentaltype
// was returned from a transaction, and the transaction was committed or rolled back.
func (d *Dentaltype) Update() *DentaltypeUpdateOne {
	return (&DentaltypeClient{config: d.config}).UpdateOne(d)
}

// Unwrap unwraps the Dentaltype entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (d *Dentaltype) Unwrap() *Dentaltype {
	tx, ok := d.config.driver.(*txDriver)
	if !ok {
		panic("ent: Dentaltype is not a transactional entity")
	}
	d.config.driver = tx.drv
	return d
}

// String implements the fmt.Stringer.
func (d *Dentaltype) String() string {
	var builder strings.Builder
	builder.WriteString("Dentaltype(")
	builder.WriteString(fmt.Sprintf("id=%v", d.ID))
	builder.WriteString(", typename=")
	builder.WriteString(d.Typename)
	builder.WriteByte(')')
	return builder.String()
}

// Dentaltypes is a parsable slice of Dentaltype.
type Dentaltypes []*Dentaltype

func (d Dentaltypes) config(cfg config) {
	for _i := range d {
		d[_i].config = cfg
	}
}
