// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"github.com/facebook/ent/dialect/sql"
	"github.com/to63/app/ent/dentalkind"
)

// Dentalkind is the model entity for the Dentalkind schema.
type Dentalkind struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Kindname holds the value of the "kindname" field.
	Kindname string `json:"kindname,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the DentalkindQuery when eager-loading is set.
	Edges DentalkindEdges `json:"edges"`
}

// DentalkindEdges holds the relations/edges for other nodes in the graph.
type DentalkindEdges struct {
	// Dentalappointment holds the value of the Dentalappointment edge.
	Dentalappointment []*Dentalappointment
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// DentalappointmentOrErr returns the Dentalappointment value or an error if the edge
// was not loaded in eager-loading.
func (e DentalkindEdges) DentalappointmentOrErr() ([]*Dentalappointment, error) {
	if e.loadedTypes[0] {
		return e.Dentalappointment, nil
	}
	return nil, &NotLoadedError{edge: "Dentalappointment"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Dentalkind) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case dentalkind.FieldID:
			values[i] = &sql.NullInt64{}
		case dentalkind.FieldKindname:
			values[i] = &sql.NullString{}
		default:
			return nil, fmt.Errorf("unexpected column %q for type Dentalkind", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Dentalkind fields.
func (d *Dentalkind) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case dentalkind.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			d.ID = int(value.Int64)
		case dentalkind.FieldKindname:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field kindname", values[i])
			} else if value.Valid {
				d.Kindname = value.String
			}
		}
	}
	return nil
}

// QueryDentalappointment queries the "Dentalappointment" edge of the Dentalkind entity.
func (d *Dentalkind) QueryDentalappointment() *DentalappointmentQuery {
	return (&DentalkindClient{config: d.config}).QueryDentalappointment(d)
}

// Update returns a builder for updating this Dentalkind.
// Note that you need to call Dentalkind.Unwrap() before calling this method if this Dentalkind
// was returned from a transaction, and the transaction was committed or rolled back.
func (d *Dentalkind) Update() *DentalkindUpdateOne {
	return (&DentalkindClient{config: d.config}).UpdateOne(d)
}

// Unwrap unwraps the Dentalkind entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (d *Dentalkind) Unwrap() *Dentalkind {
	tx, ok := d.config.driver.(*txDriver)
	if !ok {
		panic("ent: Dentalkind is not a transactional entity")
	}
	d.config.driver = tx.drv
	return d
}

// String implements the fmt.Stringer.
func (d *Dentalkind) String() string {
	var builder strings.Builder
	builder.WriteString("Dentalkind(")
	builder.WriteString(fmt.Sprintf("id=%v", d.ID))
	builder.WriteString(", kindname=")
	builder.WriteString(d.Kindname)
	builder.WriteByte(')')
	return builder.String()
}

// Dentalkinds is a parsable slice of Dentalkind.
type Dentalkinds []*Dentalkind

func (d Dentalkinds) config(cfg config) {
	for _i := range d {
		d[_i].config = cfg
	}
}
