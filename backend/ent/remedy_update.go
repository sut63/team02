// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"

	"github.com/facebook/ent/dialect/sql"
	"github.com/facebook/ent/dialect/sql/sqlgraph"
	"github.com/facebook/ent/schema/field"
	"github.com/to63/app/ent/bonedisease"
	"github.com/to63/app/ent/predicate"
	"github.com/to63/app/ent/remedy"
)

// RemedyUpdate is the builder for updating Remedy entities.
type RemedyUpdate struct {
	config
	hooks    []Hook
	mutation *RemedyMutation
}

// Where adds a new predicate for the RemedyUpdate builder.
func (ru *RemedyUpdate) Where(ps ...predicate.Remedy) *RemedyUpdate {
	ru.mutation.predicates = append(ru.mutation.predicates, ps...)
	return ru
}

// SetRemedy sets the "remedy" field.
func (ru *RemedyUpdate) SetRemedy(s string) *RemedyUpdate {
	ru.mutation.SetRemedy(s)
	return ru
}

// AddBonediseaseIDs adds the "Bonedisease" edge to the Bonedisease entity by IDs.
func (ru *RemedyUpdate) AddBonediseaseIDs(ids ...int) *RemedyUpdate {
	ru.mutation.AddBonediseaseIDs(ids...)
	return ru
}

// AddBonedisease adds the "Bonedisease" edges to the Bonedisease entity.
func (ru *RemedyUpdate) AddBonedisease(b ...*Bonedisease) *RemedyUpdate {
	ids := make([]int, len(b))
	for i := range b {
		ids[i] = b[i].ID
	}
	return ru.AddBonediseaseIDs(ids...)
}

// Mutation returns the RemedyMutation object of the builder.
func (ru *RemedyUpdate) Mutation() *RemedyMutation {
	return ru.mutation
}

// ClearBonedisease clears all "Bonedisease" edges to the Bonedisease entity.
func (ru *RemedyUpdate) ClearBonedisease() *RemedyUpdate {
	ru.mutation.ClearBonedisease()
	return ru
}

// RemoveBonediseaseIDs removes the "Bonedisease" edge to Bonedisease entities by IDs.
func (ru *RemedyUpdate) RemoveBonediseaseIDs(ids ...int) *RemedyUpdate {
	ru.mutation.RemoveBonediseaseIDs(ids...)
	return ru
}

// RemoveBonedisease removes "Bonedisease" edges to Bonedisease entities.
func (ru *RemedyUpdate) RemoveBonedisease(b ...*Bonedisease) *RemedyUpdate {
	ids := make([]int, len(b))
	for i := range b {
		ids[i] = b[i].ID
	}
	return ru.RemoveBonediseaseIDs(ids...)
}

// Save executes the query and returns the number of nodes affected by the update operation.
func (ru *RemedyUpdate) Save(ctx context.Context) (int, error) {
	var (
		err      error
		affected int
	)
	if len(ru.hooks) == 0 {
		if err = ru.check(); err != nil {
			return 0, err
		}
		affected, err = ru.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*RemedyMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = ru.check(); err != nil {
				return 0, err
			}
			ru.mutation = mutation
			affected, err = ru.sqlSave(ctx)
			mutation.done = true
			return affected, err
		})
		for i := len(ru.hooks) - 1; i >= 0; i-- {
			mut = ru.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, ru.mutation); err != nil {
			return 0, err
		}
	}
	return affected, err
}

// SaveX is like Save, but panics if an error occurs.
func (ru *RemedyUpdate) SaveX(ctx context.Context) int {
	affected, err := ru.Save(ctx)
	if err != nil {
		panic(err)
	}
	return affected
}

// Exec executes the query.
func (ru *RemedyUpdate) Exec(ctx context.Context) error {
	_, err := ru.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (ru *RemedyUpdate) ExecX(ctx context.Context) {
	if err := ru.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (ru *RemedyUpdate) check() error {
	if v, ok := ru.mutation.Remedy(); ok {
		if err := remedy.RemedyValidator(v); err != nil {
			return &ValidationError{Name: "remedy", err: fmt.Errorf("ent: validator failed for field \"remedy\": %w", err)}
		}
	}
	return nil
}

func (ru *RemedyUpdate) sqlSave(ctx context.Context) (n int, err error) {
	_spec := &sqlgraph.UpdateSpec{
		Node: &sqlgraph.NodeSpec{
			Table:   remedy.Table,
			Columns: remedy.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: remedy.FieldID,
			},
		},
	}
	if ps := ru.mutation.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if value, ok := ru.mutation.Remedy(); ok {
		_spec.Fields.Set = append(_spec.Fields.Set, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: remedy.FieldRemedy,
		})
	}
	if ru.mutation.BonediseaseCleared() {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   remedy.BonediseaseTable,
			Columns: []string{remedy.BonediseaseColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: bonedisease.FieldID,
				},
			},
		}
		_spec.Edges.Clear = append(_spec.Edges.Clear, edge)
	}
	if nodes := ru.mutation.RemovedBonediseaseIDs(); len(nodes) > 0 && !ru.mutation.BonediseaseCleared() {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   remedy.BonediseaseTable,
			Columns: []string{remedy.BonediseaseColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: bonedisease.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges.Clear = append(_spec.Edges.Clear, edge)
	}
	if nodes := ru.mutation.BonediseaseIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   remedy.BonediseaseTable,
			Columns: []string{remedy.BonediseaseColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: bonedisease.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges.Add = append(_spec.Edges.Add, edge)
	}
	if n, err = sqlgraph.UpdateNodes(ctx, ru.driver, _spec); err != nil {
		if _, ok := err.(*sqlgraph.NotFoundError); ok {
			err = &NotFoundError{remedy.Label}
		} else if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return 0, err
	}
	return n, nil
}

// RemedyUpdateOne is the builder for updating a single Remedy entity.
type RemedyUpdateOne struct {
	config
	hooks    []Hook
	mutation *RemedyMutation
}

// SetRemedy sets the "remedy" field.
func (ruo *RemedyUpdateOne) SetRemedy(s string) *RemedyUpdateOne {
	ruo.mutation.SetRemedy(s)
	return ruo
}

// AddBonediseaseIDs adds the "Bonedisease" edge to the Bonedisease entity by IDs.
func (ruo *RemedyUpdateOne) AddBonediseaseIDs(ids ...int) *RemedyUpdateOne {
	ruo.mutation.AddBonediseaseIDs(ids...)
	return ruo
}

// AddBonedisease adds the "Bonedisease" edges to the Bonedisease entity.
func (ruo *RemedyUpdateOne) AddBonedisease(b ...*Bonedisease) *RemedyUpdateOne {
	ids := make([]int, len(b))
	for i := range b {
		ids[i] = b[i].ID
	}
	return ruo.AddBonediseaseIDs(ids...)
}

// Mutation returns the RemedyMutation object of the builder.
func (ruo *RemedyUpdateOne) Mutation() *RemedyMutation {
	return ruo.mutation
}

// ClearBonedisease clears all "Bonedisease" edges to the Bonedisease entity.
func (ruo *RemedyUpdateOne) ClearBonedisease() *RemedyUpdateOne {
	ruo.mutation.ClearBonedisease()
	return ruo
}

// RemoveBonediseaseIDs removes the "Bonedisease" edge to Bonedisease entities by IDs.
func (ruo *RemedyUpdateOne) RemoveBonediseaseIDs(ids ...int) *RemedyUpdateOne {
	ruo.mutation.RemoveBonediseaseIDs(ids...)
	return ruo
}

// RemoveBonedisease removes "Bonedisease" edges to Bonedisease entities.
func (ruo *RemedyUpdateOne) RemoveBonedisease(b ...*Bonedisease) *RemedyUpdateOne {
	ids := make([]int, len(b))
	for i := range b {
		ids[i] = b[i].ID
	}
	return ruo.RemoveBonediseaseIDs(ids...)
}

// Save executes the query and returns the updated Remedy entity.
func (ruo *RemedyUpdateOne) Save(ctx context.Context) (*Remedy, error) {
	var (
		err  error
		node *Remedy
	)
	if len(ruo.hooks) == 0 {
		if err = ruo.check(); err != nil {
			return nil, err
		}
		node, err = ruo.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*RemedyMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = ruo.check(); err != nil {
				return nil, err
			}
			ruo.mutation = mutation
			node, err = ruo.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(ruo.hooks) - 1; i >= 0; i-- {
			mut = ruo.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, ruo.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX is like Save, but panics if an error occurs.
func (ruo *RemedyUpdateOne) SaveX(ctx context.Context) *Remedy {
	node, err := ruo.Save(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// Exec executes the query on the entity.
func (ruo *RemedyUpdateOne) Exec(ctx context.Context) error {
	_, err := ruo.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (ruo *RemedyUpdateOne) ExecX(ctx context.Context) {
	if err := ruo.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (ruo *RemedyUpdateOne) check() error {
	if v, ok := ruo.mutation.Remedy(); ok {
		if err := remedy.RemedyValidator(v); err != nil {
			return &ValidationError{Name: "remedy", err: fmt.Errorf("ent: validator failed for field \"remedy\": %w", err)}
		}
	}
	return nil
}

func (ruo *RemedyUpdateOne) sqlSave(ctx context.Context) (_node *Remedy, err error) {
	_spec := &sqlgraph.UpdateSpec{
		Node: &sqlgraph.NodeSpec{
			Table:   remedy.Table,
			Columns: remedy.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: remedy.FieldID,
			},
		},
	}
	id, ok := ruo.mutation.ID()
	if !ok {
		return nil, &ValidationError{Name: "ID", err: fmt.Errorf("missing Remedy.ID for update")}
	}
	_spec.Node.ID.Value = id
	if value, ok := ruo.mutation.Remedy(); ok {
		_spec.Fields.Set = append(_spec.Fields.Set, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: remedy.FieldRemedy,
		})
	}
	if ruo.mutation.BonediseaseCleared() {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   remedy.BonediseaseTable,
			Columns: []string{remedy.BonediseaseColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: bonedisease.FieldID,
				},
			},
		}
		_spec.Edges.Clear = append(_spec.Edges.Clear, edge)
	}
	if nodes := ruo.mutation.RemovedBonediseaseIDs(); len(nodes) > 0 && !ruo.mutation.BonediseaseCleared() {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   remedy.BonediseaseTable,
			Columns: []string{remedy.BonediseaseColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: bonedisease.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges.Clear = append(_spec.Edges.Clear, edge)
	}
	if nodes := ruo.mutation.BonediseaseIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   remedy.BonediseaseTable,
			Columns: []string{remedy.BonediseaseColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: bonedisease.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges.Add = append(_spec.Edges.Add, edge)
	}
	_node = &Remedy{config: ruo.config}
	_spec.Assign = _node.assignValues
	_spec.ScanValues = _node.scanValues
	if err = sqlgraph.UpdateNode(ctx, ruo.driver, _spec); err != nil {
		if _, ok := err.(*sqlgraph.NotFoundError); ok {
			err = &NotFoundError{remedy.Label}
		} else if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	return _node, nil
}
