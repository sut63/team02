// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"github.com/facebook/ent/dialect/sql/sqlgraph"
	"github.com/facebook/ent/schema/field"
	"github.com/to63/app/ent/antenatalinformation"
	"github.com/to63/app/ent/pregnancystatus"
)

// PregnancystatusCreate is the builder for creating a Pregnancystatus entity.
type PregnancystatusCreate struct {
	config
	mutation *PregnancystatusMutation
	hooks    []Hook
}

// SetPregnancystatus sets the "Pregnancystatus" field.
func (pc *PregnancystatusCreate) SetPregnancystatus(s string) *PregnancystatusCreate {
	pc.mutation.SetPregnancystatus(s)
	return pc
}

// AddAntenatalinformationIDs adds the "Antenatalinformation" edge to the Antenatalinformation entity by IDs.
func (pc *PregnancystatusCreate) AddAntenatalinformationIDs(ids ...int) *PregnancystatusCreate {
	pc.mutation.AddAntenatalinformationIDs(ids...)
	return pc
}

// AddAntenatalinformation adds the "Antenatalinformation" edges to the Antenatalinformation entity.
func (pc *PregnancystatusCreate) AddAntenatalinformation(a ...*Antenatalinformation) *PregnancystatusCreate {
	ids := make([]int, len(a))
	for i := range a {
		ids[i] = a[i].ID
	}
	return pc.AddAntenatalinformationIDs(ids...)
}

// Mutation returns the PregnancystatusMutation object of the builder.
func (pc *PregnancystatusCreate) Mutation() *PregnancystatusMutation {
	return pc.mutation
}

// Save creates the Pregnancystatus in the database.
func (pc *PregnancystatusCreate) Save(ctx context.Context) (*Pregnancystatus, error) {
	var (
		err  error
		node *Pregnancystatus
	)
	if len(pc.hooks) == 0 {
		if err = pc.check(); err != nil {
			return nil, err
		}
		node, err = pc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*PregnancystatusMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = pc.check(); err != nil {
				return nil, err
			}
			pc.mutation = mutation
			node, err = pc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(pc.hooks) - 1; i >= 0; i-- {
			mut = pc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, pc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (pc *PregnancystatusCreate) SaveX(ctx context.Context) *Pregnancystatus {
	v, err := pc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// check runs all checks and user-defined validators on the builder.
func (pc *PregnancystatusCreate) check() error {
	if _, ok := pc.mutation.Pregnancystatus(); !ok {
		return &ValidationError{Name: "Pregnancystatus", err: errors.New("ent: missing required field \"Pregnancystatus\"")}
	}
	if v, ok := pc.mutation.Pregnancystatus(); ok {
		if err := pregnancystatus.PregnancystatusValidator(v); err != nil {
			return &ValidationError{Name: "Pregnancystatus", err: fmt.Errorf("ent: validator failed for field \"Pregnancystatus\": %w", err)}
		}
	}
	return nil
}

func (pc *PregnancystatusCreate) sqlSave(ctx context.Context) (*Pregnancystatus, error) {
	_node, _spec := pc.createSpec()
	if err := sqlgraph.CreateNode(ctx, pc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (pc *PregnancystatusCreate) createSpec() (*Pregnancystatus, *sqlgraph.CreateSpec) {
	var (
		_node = &Pregnancystatus{config: pc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: pregnancystatus.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: pregnancystatus.FieldID,
			},
		}
	)
	if value, ok := pc.mutation.Pregnancystatus(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: pregnancystatus.FieldPregnancystatus,
		})
		_node.Pregnancystatus = value
	}
	if nodes := pc.mutation.AntenatalinformationIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   pregnancystatus.AntenatalinformationTable,
			Columns: []string{pregnancystatus.AntenatalinformationColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: antenatalinformation.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// PregnancystatusCreateBulk is the builder for creating many Pregnancystatus entities in bulk.
type PregnancystatusCreateBulk struct {
	config
	builders []*PregnancystatusCreate
}

// Save creates the Pregnancystatus entities in the database.
func (pcb *PregnancystatusCreateBulk) Save(ctx context.Context) ([]*Pregnancystatus, error) {
	specs := make([]*sqlgraph.CreateSpec, len(pcb.builders))
	nodes := make([]*Pregnancystatus, len(pcb.builders))
	mutators := make([]Mutator, len(pcb.builders))
	for i := range pcb.builders {
		func(i int, root context.Context) {
			builder := pcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*PregnancystatusMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, pcb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, pcb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				id := specs[i].ID.Value.(int64)
				nodes[i].ID = int(id)
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, pcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (pcb *PregnancystatusCreateBulk) SaveX(ctx context.Context) []*Pregnancystatus {
	v, err := pcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}
